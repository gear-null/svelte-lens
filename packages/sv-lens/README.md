# sv-lens

Short alias for [svelte-lens](https://npmjs.com/package/svelte-lens). Hover any Svelte 5 UI element, copy source context for AI agents.

## Install

```bash
npm install sv-lens
```

Same API as `svelte-lens` — use either package name interchangeably.

## License

MIT
